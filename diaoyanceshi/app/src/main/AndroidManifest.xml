<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diaoyan.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DesApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".recyclerviewscroll.RecyclerViewScrollActivity" />

        <activity android:name=".reflection.ReflectionActivity" />
        <activity android:name=".crashoptimizition.CrashActivity"></activity>
        <activity android:name=".crashoptimizition.OptimizationActivity" />

        <activity android:name=".videolist.ui.VideoMutilActivity" />
        <activity android:name=".keyboard.XianYuActivity" />
        <activity android:name=".dragview.DragActivity" />

    </application>

</manifest>